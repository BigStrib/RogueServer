// Optional JavaScript code to enhance functionality
const video = document.getElementById('video');

// Example: Add event listener to log when the video plays
video.addEventListener('play', () => {
    console.log('Video is playing');
});
